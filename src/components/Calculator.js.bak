class Calculator extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      squares: Array(9).fill(null),
    };
  }
  
  <>
      <CalcContainer>
        <CalcFieldsContainer>
          <CalcField><label>Number of rolls in your house</label><input type="number" min="0" value={props.noRollsInHouse} onChange={handleNoRollsInHouse.bind(this)}></input></CalcField>
          <CalcField><label>Number of wipeable toilet visits per day</label><input type="number" min="0" value="3"></input></CalcField>
          <Advanced>Advanced Settings</Advanced>
          <CalcField><label>Number of wipes per trip</label><input type="number" min="0" value="3"></input></CalcField>
          <CalcField><label>Number of sheets per wipe</label><input type="number" min="0" value="2"></input></CalcField>
          <CalcField><label>Number of sheets on a roll</label><input type="number" min="0" value="160"></input></CalcField>
          <CalcField><label>Number of people in household</label><input type="number" min="0" value="3"></input></CalcField>
        </CalcFieldsContainer>
        <About>
          <Result>
            <Prefix>Number of days before you wipe your last:</Prefix><NoSheets><label value={props.calcResult} /></NoSheets>
          </Result>
        </About>
      </CalcContainer>
    </>
  );
};

export default Calculator;

const CalcContainer = styled.div`
  margin: 0 auto;
  max-width: 120rem;
  display: flex;
  padding-top: 10rem;
`;

const About = styled.div`
  flex:50%;
  padding-left: 2.5%;
`;

const Result = styled.div`
  font-size: 4em;
`;

const Prefix = styled.div`
    color: red;
    margin-right = 1.5em;
`;

const NoSheets = styled.div`
    color: green;
`;

const CalcFieldsContainer = styled.div`
  flex: 50%;
  padding-right: 2.5%;
  justify-content: center;
  align-items: center;

`;

const Advanced = styled.div`
  color: #555;
  width: 100%;
  margin-top: 5em;
  font-size: 1.5em;
  text-align: center;
`;

const CalcField = styled.div`
  list-style-type: none;
  margin: 1em 0em;
  font-size: 2em;
  transition: all 150ms linear 0s;

  & Label {
    color: #555;
    margin-right: 1.5rem;
    text-decoration: none;
  }

  & Input {
    width: 4em;
    float: right;   
    border: none;
    border-bottom: 1px solid #666;  
    text-decoration: none;
    cursor: pointer;
    background-color: transparent;
  }
  
  &:hover {
      color: red;
    }

  &:focus {
      color: red;
    }
`;
const label = styled.div`
  font-size: 30px;
  max-width: 20rem;
  display: flex;
  margin: auto;
  padding: 1em;
`;
